let g:landscape_highlight_url =
      \ get(g:, 'landscape_highlight_url', 1)
let g:landscape_highlight_todo =
      \ get(g:, 'landscape_highlight_todo', 1)
let g:landscape_highlight_full_space =
      \ get(g:, 'landscape_highlight_full_space', 1)

